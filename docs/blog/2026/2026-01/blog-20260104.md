# XerahS Daily Blog Draft - 2026-01-04

Date: 2026-01-04
Time Zone: UTC+8
Status: Draft

## Summary

Today we focused on platform breadth and image handling by introducing a Linux platform project and migrating image processing to SkiaSharp across multiple components. We also improved region capture behaviour, added macOS app bundle support, and continued refining editor and backend gaps.

## Features

- Created an initial Linux platform project and added platform-specific build constants and init refactors to support multi-platform builds (`dd816afd`, `98f70271`).
- Migrated image handling and region types to SkiaSharp, including adding SkiaSharp dependencies in multiple projects and refactoring types to use `SKBitmap` (`0a79efd5`, `4e3aacdd`, `ea9ddfa7`, `0c6f9dbb`).
- Added a smart padding crop feature in the editor, plus various editor UI improvements like aspect ratio selection, combined export/zoom controls, and custom dark-button styles (`765ee807`, `007f2642`, `b6429a0c`, `d49a9e27`).
- Implemented macOS app bundle support and added icon assets for the Avalonia app (`6fbf63e9`).

## Fixes

- Improved region capture scaling and added debug logging to better understand capture issues across different DPI setups (`305ca0d2`).
- Cleaned up build artefacts and ensured build logs and outputs are ignored from source control, reducing noise in the repository (`db97212b`, `8695fb40`).

## Build and Tooling

- Updated README and other docs after large editor and backend changes, keeping contributors aligned with the new architecture (`f06e9160`).
- Ignored build logs and removed previous build outputs to keep the tree clean, especially important after SkiaSharp and platform changes (`db97212b`, `8695fb40`).

## Commits Reviewed

- XerahS: `dd816afd` feat: initial linux platform project setup
- XerahS: `0c6f9dbb` Add SkiaSharp dependency to Media project
- XerahS: `ea9ddfa7` Add SkiaSharp dependency to multiple projects
- XerahS: `4e3aacdd` Refactor image and region types to use SkiaSharp
- XerahS: `0a79efd5` Refactor image handling to use SkiaSharp SKBitmap
- XerahS: `98f70271` Add platform-specific build constants and refactor init
- XerahS: `765ee807` Add smart padding crop feature to image editor
- XerahS: `6fbf63e9` Add macOS app bundle support and icon assets
- XerahS: `305ca0d2` Improve region capture scaling and add debug logging
- XerahS: `8695fb40` Update .gitignore and remove build log files

## Notes

- The SkiaSharp migration is a key enabler for consistent rendering across Windows, macOS, and Linux.
- Linux and macOS platform work are progressing in parallel, so future days will continue to unify behaviour and performance across OSes.

