# XerahS Daily Blog Draft - 2026-02-05

Date: 2026-02-05
Time Zone: UTC+8
Status: Draft

## Summary

XerahS and the editor repos focused on workflow reliability, region capture fixes, and editor UI correctness. The main app bumped to v0.8.1 with workflow order preservation and region crop fixes; **ShareX.ImageEditor** fixed Export Preset and Border effect behaviour; **XerahS.Editor** aligned versioning and editor canvas preview so the host and embedded editor stay in sync.

## Features

- **XerahS: AWS SSO for Amazon S3**: Added AWS SSO authentication support for Amazon S3 so users can use SSO credentials for S3 uploads (`9e2623be`).
- **XerahS: Hotkey list UX**: Made hotkey items selectable via ListBox and improved About view layout and video UI for clearer settings navigation (`ed4fcf69`, `41dc77e8` — author date Feb 4 UTC+8).

## Fixes

- **XerahS: Region capture and AfterCapture UI**: Corrected region capture crop offset and refreshed AfterCapture UI so cropped region and post-capture flow match user selection (`c5efeabb`).
- **XerahS: Workflow order and None exclusion**: Preserved workflow order and excluded None from workflow lists so task ordering and hotkey behaviour stay consistent (`6c08b224`).
- **ShareX.ImageEditor: Export Preset and Border effect**: Fixed Export Preset menu binding and Border effect rendering, and wired `EditorCore.EffectsChanged` so the Export Preset menu and border preview update correctly (`afe0b25`, `7c6e9c7`, `103a8a1`).

## Build and Tooling

- **XerahS: Version and skill docs**: Bumped version to 0.8.1 and updated SKILL.md for release and workflow documentation (`0298fa8e`, `e5718280`).
- **XerahS.Editor: Version alignment**: Bumped XerahS.Editor version to 0.8.1 to match XerahS host (`493a277`).

## Commits Reviewed

- `9e2623be` [v0.7.7] [Feat] Add AWS SSO auth for Amazon S3
- `c5efeabb` [v0.8.0] [Fix] Correct region capture crop offset and refresh AfterCapture UI
- `0298fa8e` Bump version to 0.8.1
- `e5718280` Update SKILL.md
- `6c08b224` [v0.8.1] [Fix] Preserve workflow order and exclude None
- `afe0b25` Fix Export Preset menu binding and Border effect rendering
- `7c6e9c7` Wire EditorCore.EffectsChanged to fix Export Preset menu
- `103a8a1` Fix Outside border preview display in EditorCanvas
- `493a277` Bump version to 0.8.1 to match XerahS

## Notes

- The two commits with author date 2026-02-04 late evening UTC+8 (`ed4fcf69`, `41dc77e8`) were included where they touch the same UX theme as the Feb 5 work; strict day boundaries may assign them to 2026-02-04 in a day-only report.
- Export Preset and Border effect fixes in ImageEditor remove a class of “menu or preview not updating” bugs when effects change; follow-up testing across host and standalone editor is recommended.
