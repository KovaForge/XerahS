# XerahS Daily Blog Draft - 2026-02-24

Date: 2026-02-24
Time Zone: UTC+8
Status: Draft

## Summary

XerahS shipped v0.17.5 and v0.18.0: XIP0040 plugin architecture, Dropbox uploader and OAuth UX overhaul, Copy button and Editor window fixes, ImageEditor submodule bump for DPI/XIP0039, and docs (XIP0039, roadmap, FAQ). **ShareX.ImageEditor** added highlighter option, per-tool color/thickness, smart eraser thickness, speech balloon options, and EffectDialogRegistry (XIP0039 Pain Point 3); DPI fix for highlight/blur at 125%.

## Features

- **XerahS: XIP0040 and plugins**: Implement XIP0040 plugin architecture; add Dropbox uploader plugin; overhaul Dropbox browser OAuth UX; clarify Dropbox OAuth setup UI flow; improve Dropbox browser login and explorer start path; finalize XIP0040 implementation-ready architecture; define XIP0040 LegacySupport consolidation; expand XIP0039 cross-repo API impact; enable central package management; create PLUGIN_AND_UPLOADERS_ARCHITECTURE_ANALYSIS.md (`10ae6ef6`, `a5100cb7`, `e79288d8`, `a3cb180e`, `3a698358`, `c323722e`, `81f7efa0`, `5a280efe`, `ba1b3ac2`, `f999beba`).
- **ShareX.ImageEditor: XIP0039**: Introduce EffectDialogRegistry for dialog centralization (Pain Point 3); refactor: guardrails + split monoliths + effect registry (Pain Points 1–3); add highlighter option and ViewModel support; use FillColor for highlighter and tidy updates; Highlight: use FillColor and rename option; add Smart Eraser thickness option; add per-tool color and thickness options; skip syncing stroke/shadow for effect annotations; add speech balloon options and text sizing (`658b102`, `8c42719`, `0e945a6`, `1435846`, `c28d988`, `4028d6a`, `26bb6bf`, `16bc352`, `dd6efc7`).
- **XerahS: Docs**: Refine XerahS pronunciation wording in FAQ; update ShareX framework to .NET 9.0 in FAQ; expand XIP0039 ImageEditor refactor safety scan; refresh roadmap and walkthrough docs (`a1ed582e`, `2ae6247d`, `10f3e6c6`, `4a2c2ddc`).

## Fixes

- **XerahS**: Fix Copy button copies original image instead of edited image (fixes #147); Fix Editor window does not open automatically after screenshot (fixes #147); Fix Windows screen recording availability check and Auto plugin config log; Update EditorOptions.FontSize to TextFontSize for ImageEditor compatibility; ensure Linux daemon executable mode; correct version back to 0.18.0; remove dead legacy uploader code paths (`bdd1480c`, `0465197c`, `8dbbc3d9`, `69065ea1`, `10ae6ef6`, `faa35b18`, `fd89ab87`, `38e05f47`).
- **ShareX.ImageEditor**: Fix ImageEditor highlight/blur at 125% DPI; add version to window title (`c7a16f2`).
- **XerahS: XIP0039 host**: Bump ImageEditor submodule after DPI/XIP0039 merge; bump ImageEditor submodule to Pain Point 3 commit; host glue: remove reflection, re-enable rotate tests, bump submodule; update ImageEditor submodule; Directory.Build.props (`800e0fb4`, `450d345b`, `d369814d`, `849c1d6e`, `bdd1480c`).
- **XerahS: Merge**: Resolve merge and keep DPI fixes with XIP0039 baseline (`6b83028`).

## Build and Tooling

- **XerahS**: [v0.17.5] [Chore] Bump version to 0.17.5; [v0.17.4] [Docs] Refresh roadmap and walkthrough docs (`2e5263ed`, `4a2c2ddc`).

## Commits Reviewed

(Full set from git log 2026-02-24 UTC+8: XerahS and ShareX.ImageEditor as listed in repo logs.)

## Notes

- XIP0040 and Dropbox plugin establish the plugin architecture and first-party uploader UX.
- EffectDialogRegistry and per-tool options improve ImageEditor usability and DPI behavior.
