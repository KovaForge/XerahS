# XerahS Daily Blog Draft - 2026-02-10

Date: 2026-02-10
Time Zone: UTC+8
Status: Draft

## Summary

XerahS improved the Index Folder experience (open in its own window, remove WebView and open HTML in system browser), fixed duplicate date in log filename on date rotation, and enabled the Open in Browser button after indexing.

## Features

- **XerahS: Index Folder**: Make Index Folder open in its own window; remove WebView and open HTML in system browser so indexing opens in the default browser (`8b20b3b1`, `16945a05`).

## Fixes

- **XerahS: Logging and indexer**: Fix duplicate date in log filename on date rotation; enable Open in Browser button after indexing (`69cb3c2a`, `45825293`).

## Build and Tooling

- None (no separate build/tooling commits this day).

## Commits Reviewed

- `16945a05` [v0.14.3] [Fix] Remove WebView and open HTML in system browser
- `8b20b3b1` [v0.14.2] [Feature] Make Index Folder open in its own window
- `69cb3c2a` [v0.14.1] [Fix] Fix duplicate date in log filename on date rotation
- `45825293` [v0.14.3] [Fix] Enable Open in Browser button after indexing

## Notes

- Moving index HTML to the system browser simplifies the stack and improves compatibility with user preferences.
