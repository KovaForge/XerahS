# XerahS Daily Blog Draft - 2026-02-25

Date: 2026-02-25
Time Zone: UTC+8
Status: Draft

## Summary

XerahS merged XIP0039 ImageEditor refactor, added DestinationsPluginSdk and plugin template, FTP/FTPS/SFTP and Pastebin plugins, uploader legacy cleanup, and Linux fixes (portal Watch*, GStreamer videocrop, slurp path). **ShareX.ImageEditor** added text style (bold/italic/underline), active tool icon in toolbar, shadow effect for text, number annotation drag, Shift+snap 45° for lines/arrows, and removed legacy per-effect events (XIP0039).

## Features

- **XerahS: Plugin SDK and uploaders**: Add XerahS.DestinationsPluginSdk (lightweight plugin contracts); wire SDK into solution and project references; use SDK contracts in XerahS.Uploaders; remove duplicated plugin types; cast IUploaderProvider.CreateInstance result to Uploader at call sites; add plugin template and developer guide for DestinationsPluginSdk; uploaders: LegacySupport flatten, Chevereto removal, legacy enums deprecated, plugin auto-discovery; add FTP / FTPS / SFTP uploader plugin; add Pastebin text uploader plugin; rename SDK to XerahS.UploaderPluginSdk (one plugin = one uploader) (`546112b5`, `7d67bb52`, `df44f365`, `f57c0e0a`, `e776e1a0`, `55b769b3`, `54c25ecc`, `9c52a6b6`, `f5c75dd5`, `8da90dcf`).
- **XerahS: Linux**: Fix Linux portal Watch* errors to prevent unobserved task exception; clamp GStreamer videocrop left/top to >= 0; resolve slurp path to avoid 'No such file or directory' on region selection (`c98bd02c`, `d88a1f3d`, `3ab2c541`).
- **ShareX.ImageEditor**: Add text style (bold/italic/underline) support; use icon font for text style buttons; center text rendering and editing alignment; show active tool icon in toolbar; add shadow effect to text annotations; add ActiveToolName and tooltip binding; allow dragging number annotation after insert; add Shift+snap-to-45° for lines/arrows; refactor sample image loader and generator; move AvaloniaIntegration.cs to Helpers; start EditorWindow maximized by default; skip syncing stroke for image/effect annotations; clear IsDirty flag after initial image load; restrict ShowTextStyle to Text tool; set VM text style flags from TextAnnotation; remove direct pointer overrides from EditorCanvas; defer popup close to avoid null PlatformImpl; remove text shadow support; invalidate OutlinedTextControl on font change (`528bf51`, `1b5db86`, `af7e11d`, `b874062`, `7244eca`, `62ffece`, `4be126c`, `7a6e5d0`, `9cb3676`, `b8ad23f`, `9505a6c`, `815b042`, `ffc899d`, `a31c672`, `6056a06`, `a76a87f`, `9411d48`, `a3dfb54`, `91598d7`).
- **ShareX.ImageEditor (XIP0039)**: Remove legacy per-effect events from EffectsMenuDropdown; merge PR #5 feature/XIP0039-ImageEditor-refactor (`1580fdc`, `87128be`).

## Fixes

- **XerahS**: Close-to-tray: ensure tray visible when hiding; clarify comments (`b387ca95`).
- **XerahS: gitignore and Directory.Build.props**: gitignore: ignore build/IDE artifacts at any depth; update Directory.Build.props (`f172e6e3`, `ae6d3f81`).

## Build and Tooling

- **XerahS**: [XIP0039] Bump ImageEditor submodule: remove legacy per-effect events; merge PR #150 feature/XIP0039-ImageEditor-refactor; [Task] Move XIP0039 ImageEditor refactor to complete; [Docs] Move XIP0040 to tasks/complete with implementation verification; update ImageEditor submodule (`8da90dcf`, `1f25a177`, `94f0265a`, `e98666b8`, `03691dec`, `432b0a4b`).

## Commits Reviewed

(Full list from git log 2026-02-25 UTC+8 for XerahS and ShareX.ImageEditor.)

## Notes

- DestinationsPluginSdk and plugin template lower the bar for third-party uploaders.
- XIP0039 completion (legacy per-effect events removal) simplifies ImageEditor effect wiring.
