# XerahS Daily Blog Draft - 2026-03-28

Date: 2026-03-28
Time Zone: UTC+8
Status: Draft

## Summary

March 28 was a release-hardening and tooling day anchored around the v0.21.0 cut. ShareX.ImageEditor repaired overlay crop integration, then disabled pin and upload actions in the empty editor state; XerahS immediately pulled those fixes through the submodule and adjusted its own host-side empty-state action handling to match. The rest of the day focused on release and documentation quality: the release script was updated for v0.21.0, the changelog was expanded and then consolidated by theme, and the repo gained a scheduled workflow that keeps the current and previous UTC+8 blog drafts created automatically on `develop`.

## Features

- XerahS: Blog-maintenance automation became a committed repo capability instead of a manual local routine. The new scheduled workflow runs daily at `16:00 UTC` on `develop`, calls the `draft-blog-post` wrapper with `-IncludePreviousDay`, and commits missing draft files for both the current and previous UTC+8 days when needed (`1a9e1eb4`).
- XerahS: Changelog tooling became more opinionated and useful for release work. The `update-changelog` skill and script can now consolidate similar commits automatically, which fits the same day's changelog rewrite toward grouped, thematic entries instead of long one-line lists (`4ba4de68`, `81354e19`, `e34b26a1`).

## Fixes

- ShareX.ImageEditor: Overlay crop integration and the editor build were repaired together, undoing a broken path in the interaction controllers and wiring the editor view back into the corrected crop flow instead of leaving the branch in a partially merged state (`12c9331`, `2a39990`).
- ShareX.ImageEditor / XerahS: Empty-state actions were tightened so pin and upload affordances are disabled when no image is loaded. The fix landed upstream in ShareX.ImageEditor first, then XerahS updated its submodule pointer and host-side empty-state handling so the desktop app no longer advertises invalid actions on a blank editor surface (`f000e96`, `05b24ba1`, `334822f7`).

## Build and Tooling

- XerahS: The release pipeline moved to the actual `v0.21.0` cut. The CI-side release commit updated the version-bump and tag script, which made the repo's release automation match the version already declared in `Directory.Build.props` (`335ff31b`).
- XerahS: Release documentation was rebuilt in two passes. The repo first backfilled `docs/CHANGELOG.md` from `v0.20.12` through the `v0.21.0` pre-release range, then condensed the result by theme so the changelog reads as grouped release notes instead of a raw commit stream (`4ba4de68`, `81354e19`).
- XerahS: The changelog skill itself was updated the same day so future changelog refreshes can auto-merge similar commit subjects, reducing the amount of manual cleanup needed after a release or release-candidate pass (`e34b26a1`).

## Commits Reviewed

XerahS:

- `1a9e1eb4` `chore: schedule daily blog draft upserts on develop`
- `e34b26a1` `[v0.21.0] [Docs] Auto-consolidate similar commits in update-changelog skill and script.`
- `81354e19` `[v0.21.0] [Docs] Condense v0.21.0 changelog entries by theme.`
- `4ba4de68` `[v0.21.0] [Docs] Update CHANGELOG from v0.20.12 through v0.21.0 pre-release.`
- `335ff31b` `[v0.21.0] [CI] Release v0.21.0`
- `334822f7` `[v0.21.0] [Fix] Update image editor empty-state actions`
- `05b24ba1` `[v0.21.0] [Library] Sync ShareX.ImageEditor submodule to upstream develop.`

ShareX.ImageEditor:

- `f000e96` `[Fix] Disable empty-state pin and upload actions`
- `2a39990` `Merge pull request #12 from ShareX/codex/restore-overlay-crop-commits`
- `12c9331` `[Fix] Restore overlay crop integration and editor build`

XerahS.Editor:

- No local checkout was available in this workspace, so no verifiable XerahS.Editor commits were reviewed for 2026-03-28.

## Notes

- March 28 was much more about release readiness and maintenance than net-new end-user surface area. The main shipped behavior change was fixing ImageEditor crop and empty-state interactions while the rest of the work improved release notes, tooling, and automation around the repo.
- The new scheduled blog workflow only guarantees that draft files exist; it does not replace the commit-grounded writing pass that fills these posts with real Summary, Features, Fixes, and Notes content.
- No local `XerahS.Editor` repository was present in this workspace, so this daily post is grounded in verified XerahS and ShareX.ImageEditor history only.
