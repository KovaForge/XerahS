# XerahS Daily Blog Draft - 2026-04-27

Date: 2026-04-27
Time Zone: UTC+8
Status: Draft

## Summary

2026-04-27 focused on correctness and cleanup in the core desktop flows: hotkey duplication and unregistration, pinned-image bitmap lifetime, FFmpeg audio defaults, clipboard service cleanup, and Immich album persistence all received targeted fixes. The day also included a ShareX.VideoEditor submodule bump and a version advance, but the verified feature work itself was primarily stabilization rather than new surface area.

The verified history for this UTC+8 day came from the main XerahS repository. `ShareX.ImageEditor` had no same-day commits in the checked-out submodule, and `XerahS.Editor` was still not available locally to inspect.

## Features

- XerahS: Added diagnostic logging when hotkey unregistration fails inside `WorkflowManager`, giving contributors and advanced users more actionable telemetry when global shortcut cleanup goes wrong.
- XerahS: Updated the `ShareX.VideoEditor` submodule reference so the multimedia toolchain can pick up the latest editor-side changes without waiting for a larger synchronization batch.

## Fixes

- XerahS: Fixed duplicated hotkey commands to deep-copy `TaskSettings` instead of reusing shared references, preventing one shortcut's task changes from leaking into another command.
- XerahS: Fixed `PinToScreenManager` callers to dispose `SKBitmap` instances correctly, reducing repeated bitmap lifetime leaks across the pin-to-screen flow and its related UI entry points.
- XerahS: Fixed the default FFmpeg audio codec from the invalid `libvoaacenc` value to `aac`, which removes a broken default from screen-recording configuration paths.
- XerahS: Fixed three clipboard and UI cleanup bugs at once by closing a process-leak-on-exception path, cleaning up a temporary-file leak, and resolving a conflicting `using var` pattern in clipboard-related code.
- XerahS: Fixed Immich album placeholder handling so persisted `AlbumName`-only state keeps a usable placeholder name instead of dropping the intended album label.

## Build and Tooling

- XerahS: Bumped the app version to `0.22.92` as the day's stabilization fixes accumulated and the branch advanced toward the next unreleased build.
- XerahS: The FFmpeg codec fix and clipboard cleanup work were paired with review-tracker updates during verification, helping document why those fixes were required and what follow-up checks were run.
- XerahS: No same-day `ShareX.ImageEditor` code changes were present locally, so today's combined record is effectively an XerahS-only post apart from the `ShareX.VideoEditor` submodule pointer update.

## Commits Reviewed

- `063bbd40` - Fix hotkey duplicate command to deep-copy TaskSettings instead of sharing reference
- `ae77f591` - [v0.22.89] [Core] Add diagnostics logging when hotkey unregistration fails in WorkflowManager
- `c05298e2` - Fix SKBitmap resource leaks in PinToScreenManager callers
- `d4ffe767` - Fix SKBitmap resource leaks in PinToScreenManager callers
- `13a54f58` - Fix FFmpegAudioCodec default from libvoaacenc (invalid) to aac
- `6b3fe481` - Fix three clipboard/UI bugs: process leak on exception, temp file leak, and using var conflict
- `779f641f` - Fix Immich album placeholder name loss when only AlbumName is persisted
- `26fc5e49` - chore: update ShareX.VideoEditor to
- `1d08fb88` - Bump version to 0.22.92

## Notes

- `ShareX.ImageEditor` had no verified same-day commits for this UTC+8 day, and `XerahS.Editor` was still unavailable locally, so this entry is grounded almost entirely in the main XerahS history.
- The current working tree was clean while drafting this post, which means no uncommitted same-day changes were available to include beyond the verified git history.
- This was a maintenance-heavy day that improved correctness in hotkeys, recording defaults, image lifetime handling, clipboard cleanup, and uploader configuration without introducing a large new user-facing feature.

