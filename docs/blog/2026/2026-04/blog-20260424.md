# XerahS Daily Blog Draft - 2026-04-24

Date: 2026-04-24
Time Zone: UTC+8
Status: Draft

## Summary

April 24 continued the cleanup of stale uploader and settings state, with fixes landing across upload fallback selection, custom uploader bindings, reset behavior, and SFTP credential recovery. The day also tightened mobile hotkey cleanup, CLI record-region validation, Imgur album reload behavior, and toast URL handling so UI and command-line paths stay in sync under stale or partially reset state.

## Features

- None beyond maintenance and hardening work.

## Fixes

- XerahS: Fixed unavailable uploader selection fallback and switched custom uploader bindings to prefer live definitions so stale instances do not keep routing uploads to the wrong target (`653e8748`, `b9e55839`).
- XerahS: Fixed reset flow cleanup for stale recent tasks and restored SFTP password fallback when the saved key path is no longer valid (`b3fe0ba3`, `0a78949c`).
- XerahS: Fixed stale Imgur album selection reload so refreshed provider state no longer leaves the UI pointed at an outdated album selection (`3a5a642b`).
- XerahS: Cleaned up mobile hotkey state more reliably to avoid stale registrations persisting through lifecycle transitions (`db50bc46`).
- XerahS: Tightened CLI record-region validation so invalid region arguments are rejected more predictably before recording starts (`302ebff7`).
- XerahS: Fixed markdown image links embedded in toast URLs so routed clicks preserve the correct target instead of breaking on markdown-formatted content (`2e89df18`).

## Build and Tooling

- XerahS: Added focused regression coverage for uploader fallback selection, custom uploader bindings, settings reset cleanup, SFTP fallback behavior, mobile hotkey lifecycle cleanup, CLI record parsing, and toast click routing to lock the fixes in with tests (`653e8748`, `b9e55839`, `b3fe0ba3`, `0a78949c`, `db50bc46`, `302ebff7`, `2e89df18`).
- XerahS: Kept the hourly review tracker current while the day's fixes were landing (`dd697e6f`).

## Commits Reviewed

- `XerahS` `653e8748` `Fix unavailable uploader selection fallback`
- `XerahS` `b9e55839` `fix: prefer live custom uploader bindings`
- `XerahS` `b3fe0ba3` `Fix reset clearing stale recent tasks`
- `XerahS` `0a78949c` `Fix SFTP password fallback when key path is stale`
- `XerahS` `3a5a642b` `Fix stale Imgur album selection reload`
- `XerahS` `db50bc46` `Fix mobile hotkey state cleanup`
- `XerahS` `302ebff7` `Fix CLI record region validation`
- `XerahS` `2e89df18` `Fix markdown image links for toast URLs`
- `XerahS` `dd697e6f` `Update hourly review tracker`

## Notes

- ShareX.ImageEditor had no commits on this UTC+8 day in the local submodule history, and the remote XerahS.Editor history also had no matching commits for the same UTC+8 window.
- This post reflects the commits present on `develop` at drafting time for April 24 and may need another pass later if more same-day work lands before the UTC+8 day closes.
