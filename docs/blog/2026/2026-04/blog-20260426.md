# XerahS Daily Blog Draft - 2026-04-26

Date: 2026-04-26
Time Zone: UTC+8
Status: Draft

## Summary

2026-04-26 turned into a concentrated XerahS maintenance and release day, with several user-facing fixes landing across macOS service execution, region capture window filtering, FFmpeg recording output handling, and Nextcloud folder creation. The repo then rolled through a rapid release sequence from `v0.22.64` to `v0.22.76`, indicating that the fixes were packaged and published continuously as they landed.

The verified activity for this UTC+8 day came from the main XerahS repository. `ShareX.ImageEditor` and `XerahS.Editor` were not available as local sibling checkouts in this workspace, so this post is grounded in the repository history that could be directly inspected here.

## Features

- XerahS: Expanded the regression-test surface around macOS process startup, region capture window detection, FFmpeg recording output paths, and Nextcloud upload folder resolution, giving the desktop codebase stronger coverage for platform-specific edge cases and recent bug fixes.
- XerahS: The release pipeline continuously published the day's fixes through a fast sequence of tagged builds, making the maintenance work available in incremental `v0.22.64` through `v0.22.76` releases instead of waiting for a single larger drop.

## Fixes

- XerahS: Fixed macOS service command timeout handling so spawned system-service commands now preserve intended timeout behavior instead of expiring incorrectly under the new process-start path.
- XerahS: Corrected logical window filtering in region capture so excluded or non-target windows are handled more reliably during window discovery.
- XerahS: Fixed FFmpeg recording output path handling and added dedicated coverage so generated recording targets keep the expected destination path behavior.
- XerahS: Fixed blank Nextcloud folder creation so upload requests no longer lose the intended remote folder when the configured path is empty or normalized at runtime.

## Build and Tooling

- XerahS: Published a rapid CI-driven release train from `v0.22.64` through `v0.22.76`, updating versioned release assets and changelog/package metadata throughout the morning.
- XerahS: Added targeted regression tests for macOS system-service startup, region capture window detection, FFmpeg recording output paths, and Nextcloud provider behavior to reduce the chance of these fixes regressing in later releases.
- XerahS: Merged the latest `upstream/develop` changes into the local `develop` branch, and the docs automation also refreshed same-day daily blog drafts while the release train was running.

## Commits Reviewed

- `d3538a50` - Fix macOS service command timeouts
- `1ba909b9` - [Docs] Ensure current and previous UTC+8 blog drafts.
- `82a58b30` - Merge remote-tracking branch 'upstream/develop' into develop
- `5ffc730b` - Fix logical window filtering in region capture
- `ee3e62ca` - Fix FFmpeg recording output path handling
- `223475ad` - Fix blank Nextcloud folder creation
- `aeadf1ad` - [v0.22.64] [CI] Release v0.22.64
- `567a9857` - [v0.22.65] [CI] Release v0.22.65
- `52594375` - [v0.22.66] [CI] Release v0.22.66
- `54b7f678` - [v0.22.67] [CI] Release v0.22.67
- `98a03905` - [v0.22.68] [CI] Release v0.22.68
- `338eba49` - [v0.22.69] [CI] Release v0.22.69
- `86ca2b32` - [v0.22.70] [CI] Release v0.22.70
- `eb5817a2` - [v0.22.71] [CI] Release v0.22.71
- `0cdc5515` - [v0.22.72] [CI] Release v0.22.72
- `96ba730f` - [v0.22.73] [CI] Release v0.22.73
- `d7976e91` - [v0.22.74] [CI] Release v0.22.74
- `b436bc7e` - [v0.22.75] [CI] Release v0.22.75
- `b92247c2` - [v0.22.76] [CI] Release v0.22.76
- `3acabb15` - [v0.22.76] [Docs] Add 2026-04-25 engineering blog draft.
- `d48779c8` - [v0.22.76] [Docs] Add 2026-04-26 quiet day blog draft.

## Notes

- The verified work for this day was concentrated in XerahS itself; `ShareX.ImageEditor` and `XerahS.Editor` were not locally available to inspect for same-day history.
- Earlier in the morning, the daily blog automation recorded this as a quiet day before the later fixes and releases landed, so the post needed a same-day refresh to match the eventual verified history.
- This was primarily a stabilization and shipping day rather than a feature-heavy one, with most of the value coming from bug fixes, regression coverage, and release packaging throughput.
