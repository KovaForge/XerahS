# XerahS Daily Blog Draft - 2026-04-22

Date: 2026-04-22
Time Zone: UTC+8
Status: Draft

## Summary

April 22 focused on path normalization, uploader correctness, and editor contract cleanup. XerahS fixed temp-file naming, file-save edge cases, OCR and Nextcloud flows, provider-context refresh, workflow duplication identity, overlay preferences, and several platform-specific path issues. ShareX.ImageEditor also aligned its editor dialog contracts with the current tests, and the host repo restored editor-history test coverage around the same time. XerahS.Editor was checked via the remote `develop` history for the same UTC+8 window and did not add any matching commits.

## Features

- XerahS: Made toast actions emit real markdown image links, improving how image actions are surfaced back through the app (`82e69b19`).

## Fixes

- XerahS: Fixed CLI upload temp-filename sanitization and cleanup (`ef6cb6fb`, `3da58ae2`).
- XerahS: Fixed leaf-filename bitmap saves and leaf-only async index output paths (`51d32964`, `810b797b`).
- XerahS: Corrected Wayland portal directory URIs and hardened system-service path dispatch (`0ac6bad3`, `ed23dfc5`).
- XerahS: Repaired XSIE preset serialization metadata and finished the editor overlay contract cleanup (`2ce7e44e`, `93b22f8e`).
- XerahS: Hardened OCR and legacy Nextcloud config flows, uploader routing, custom-uploader reload path normalization, provider-context secret refresh, malformed Imgur token handling, and malformed FTP enum normalization (`4c878a87`, `ad7a7127`, `c590c34d`, `0f2509ba`, `117329fa`, `c088f124`).
- XerahS: Fixed missing screen-recording output validation, workflow duplication task identity, clipboard-menu visibility for non-image items, and Linux overlay-selector preference normalization (`dce851cd`, `f1f03d42`, `fa792ac3`, `612f2c6e`).
- ShareX.ImageEditor: Aligned editor dialog contracts with the active test suite (`8bd5399`).

## Build and Tooling

- XerahS: Restored editor-history coverage after the overlay and editor cleanup (`6457f621`).
- XerahS: Merged upstream `develop` while the fixes were landing (`708c2d50`).
- XerahS: Continued the daily docs-draft maintenance flow (`32be1a34`, `d9df98ad`).

## Commits Reviewed

- `ShareX.ImageEditor` `8bd5399` `fix: align editor dialog contracts with tests`
- `XerahS` `ef6cb6fb` `Fix CLI upload temp filename sanitization`
- `XerahS` `3da58ae2` `Fix CLI upload temp directory cleanup`
- `XerahS` `51d32964` `Fix leaf filename bitmap saves`
- `XerahS` `82e69b19` `fix: emit real markdown image links from toast actions`
- `XerahS` `0ac6bad3` `Fix Wayland portal directory URIs`
- `XerahS` `2ce7e44e` `Fix XSIE preset serialization metadata`
- `XerahS` `4c878a87` `fix: harden OCR and legacy Nextcloud config flows`
- `XerahS` `93b22f8e` `fix: finish editor overlay contract cleanup`
- `XerahS` `c590c34d` `Fix custom uploader reload path normalization`
- `XerahS` `dce851cd` `Fix missing screen recording output validation`
- `XerahS` `6457f621` `test: restore editor history coverage`

## Notes

- XerahS.Editor was checked via remote `develop` history for this UTC+8 day and had no matching commits.
- April 22 was still firmly in stabilization mode, but the toast markdown-link change stands out as the one clearly user-visible polish item in the batch.
