# XerahS Daily Blog Draft - 2026-04-28

Date: 2026-04-28
Time Zone: UTC+8
Status: Draft

## Summary

2026-04-28 was a dense XerahS stabilization day that started with upload-history, uploader-provider, Wayland, notification, media, and compare-capture fixes, then continued into assistant, MCP, capture-region, and settings-upgrade hardening later in the evening. The same UTC+8 day also refreshed app branding assets and reorganized the destination-plugin documentation so the public listing, registry guidance, and plugin index sit under a clearer source-of-truth layout.

All verified same-day activity came from the main XerahS repository. `ShareX.ImageEditor` and the available local `XerahS.Editor` checkout had no matching author-dated commits for this UTC+8 day.

## Features

- XerahS: Added a community destination-plugin listing under `plugins/destinations/README.md` so contributors and users have a single visible entry point for plugins such as Pixelfox alongside the authoritative registry/index files.
- XerahS: Moved the destination-plugin registry documentation and index into the destination-plugin structure, making the docs and published plugin metadata easier to follow and maintain together.
- XerahS: Updated the About view to use the ShareX icon and refreshed the application logo assets so branding is more consistent across shell and in-app surfaces.

## Fixes

- XerahS: Fixed assistant history basename fallback, normalized assistant OCR options, and corrected assistant screenshot history paging so assistant-driven capture and history workflows behave more predictably across naming, OCR, and pagination paths.
- XerahS: Fixed MCP string parameter validation and the headless-editor unavailable contract so automation callers get stricter input validation and a clearer unavailable response instead of ambiguous failures.
- XerahS: Fixed editor session source snapshots and preserved upload-result URLs in history so edited captures and uploaded outputs stay traceable in the user's history.
- XerahS: Fixed provider and uploader edge cases across Nextcloud `PROPFIND` parsing, FTP port normalization, and Imgur explorer paging, reducing brittle behavior across several destination integrations.
- XerahS: Fixed duplicate plugin load-context replacement so repeated plugin activation no longer risks conflicting runtime state.
- XerahS: Fixed Wayland portal shortcut snapshot refresh, KDE Wayland geometry parsing, Linux clipboard owner-process disposal, and monitor stop-timeout handling, tightening several Linux desktop capture and process-lifecycle problems.
- XerahS: Fixed toast right-click execution and flyout opacity handlers so notification interactions now trigger the expected command paths without broken visual-state handling.
- XerahS: Fixed async indexer empty-folder filtering and folder-size reporting, guarded `HandleExistsFile` cancel/ask outcomes, and corrected settings-upgrade version detection so maintenance and migration flows do not drift on edge cases.
- XerahS: Fixed FFmpeg video-info quoting, video-helper dithering defaults, thumbnail time-slice selection, screenshot-combine leak paths, workflow region-timeout deregistration order, compare-capture region validation, and configured capture-region bounds for stronger media and capture correctness.

## Build and Tooling

- XerahS: Bumped the app version to `0.22.113` early in the day, then continued landing same-day fix commits with matching tracker updates and regression tests instead of masking issues behind warning-policy changes.
- XerahS: Refreshed `Logo.png`, regenerated `Logo.ico` with multiple resolutions, and pulled in updated ShareX icon assets to improve packaging and branding fidelity.
- XerahS: Reworked destination-plugin documentation by renaming `developers/plugins-destinations` to `developers/destination-plugins`, moving registry docs, fixing stale references, and refining the public Pixelfox listing format.
- XerahS: Added or expanded targeted tests for assistant history/service behavior, MCP server contracts, capture-stage region validation, Wayland geometry parsing, and settings-path/version-upgrade handling.

## Commits Reviewed

- `8e43d834` - Fix assistant history basename fallback
- `5e4cd4ad` - Fix MCP string param validation
- `db7c97f1` - Fix editor session source snapshot
- `1a5beb4e` - Preserve upload result URLs in history
- `4bd3c229` - Fix Nextcloud PROPFIND propstat parsing
- `1ef0fa55` - Fix duplicate plugin load context replacement
- `84b153fd` - Fix FTP uploader port normalization
- `6db09fa8` - Clamp Imgur explorer page sizes
- `243f1c69` - Fix Wayland portal shortcut snapshot refresh
- `bd5dc257` - Update app logo and ShareX icon assets
- `e6a679cd` - Merge upstream/develop: Update app logo and ShareX icon assets
- `ef2390c5` - Fix VideoHelpers: default dither to sierra2_4a to match ShareX upstream
- `9a1ff6b0` - Fix ToastWindow: wire right-click to ExecuteRightClick() and fix flyout menu opacity handlers
- `c91330c3` - IndexerAsync: fix IgnoreEmptyFolders and folder-size stats for async text output
- `959a8dcd` - Core: guard HandleExistsFile Cancel/Ask results across task/file write paths
- `91346c8d` - Bump version to 0.22.113
- `9178a566` - fix(Media): FFmpegCLIManager GetVideoInfo missing closing quote in FFmpeg args
- `8d84ee7c` - Update Logo.ico
- `9de7ffe1` - Regenerate Logo.ico with multi-resolution sizes (16/32/48/64/128/256)
- `d2c0798e` - Fix WorkflowCommand region-override timeout handler deregistration order
- `012614ee` - Fix VideoThumbnailer.CombineScreenshots early-return leak in inner loop
- `431a4048` - Fix Linux clipboard owner process disposal and monitor stop timeout handling
- `35d1087a` - Use ShareX icon in AboutView
- `ff255f69` - docs: rename plugins-destinations to destination-plugins
- `331ae6a4` - docs: add community destination plugins listing
- `6a83d72a` - Fix compare-capture region validation
- `579d2a1a` - [v0.22.118] [Docs] Move destination plugin registry docs
- `53fe285c` - docs: fix stale plugins-destinations reference in developers/README.md
- `336bb8b6` - docs: improve Pixelfox plugin formatting in plugins/destinations/README.md
- `63dc3c08` - docs: revert Pixelfox formatting to bullet list
- `911a80f2` - Fix MCP headless editor unavailable contract
- `cc16b61a` - Harden KDE Wayland geometry parsing
- `094f1463` - Merge remote-tracking branch 'upstream/develop' into develop
- `6a861cc1` - Normalize assistant OCR options
- `e3e44c86` - Fix settings upgrade version detection
- `776075a1` - Fix assistant screenshot history paging
- `de30bace` - Guard configured capture region bounds

## Notes

- This was an XerahS-only committed-history day even though all three repositories were checked. `ShareX.ImageEditor` and the available local `XerahS.Editor` checkout had no qualifying 2026-04-28 UTC+8 commits.
- The destination-plugin work remained mostly documentation and registry-structure cleanup on this date, so the immediate user-visible outcome was better discoverability and a cleaner source-of-truth layout rather than a new plugin-management UI.
- Many fixes were paired with tests and hourly tracker updates, which suggests a deliberate audit-and-stabilize pass rather than isolated one-off patches.
