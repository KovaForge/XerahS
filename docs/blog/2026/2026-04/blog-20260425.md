# XerahS Daily Blog Draft - 2026-04-25

Date: 2026-04-25
Time Zone: UTC+8
Status: Draft

## Summary

2026-04-25 focused on tightening correctness across the XerahS desktop app, CLI surface, uploader integrations, assistant cache flow, and supporting tooling, with a steady run of small but well-tested fixes landing through the full UTC+8 day. The work covered workflow validation, toast timing safeguards, file path mutation handling, recording history preservation, Linux startup integration, excluded window detection, clipboard image decoding, Nextcloud and SFTP handling, editor bitmap lifetime fixes, onboarding parsing, MCP validation, platform notification timeouts, and capture timing edge cases.

The only verified activity for this UTC+8 day came from the main XerahS repository. `ShareX.ImageEditor` and `XerahS.Editor` were not available as local sibling checkouts in this workspace, so this post is grounded in the repository history that could be directly inspected here.

## Features

- XerahS: Hardened the CLI workflow command so region overrides reuse the existing capture-region parser and invalid durations fail early with clearer validation paths instead of surfacing malformed geometry or timeout arithmetic problems later.
- XerahS: Split Windows clipboard DIB decoding into a dedicated `ClipboardDibCodec` implementation, giving the platform layer a clearer seam for future clipboard image work while restoring automated coverage around the supported bitmap formats.
- XerahS: Added stricter MCP object parameter validation with dedicated regression coverage, improving the reliability of assistant- and automation-driven flows that depend on structured object arguments.

## Fixes

- XerahS: Fixed toast timing validation so invalid timing values are rejected more reliably and the click-routing test suite now covers the safer configuration path.
- XerahS: Corrected file path mutation logic so extension handling stays stable when file names are transformed, then tightened unique path suffix handling later in the day so generated names preserve the intended shape across more collision cases.
- XerahS: Preserved the uploader host when recording task history and fixed fallback host attribution, so upload and recording history retain the source host context more consistently.
- XerahS: Escaped Linux startup desktop entry values correctly and added dedicated Linux startup tests to lock in the expected `.desktop` output.
- XerahS: Fixed stale excluded window queries in region capture by tightening window detection behavior and adding regression coverage for the updated service.
- XerahS: Normalized Nextcloud relative paths correctly and hardened blank-folder creation so cloud uploads no longer lose the intended remote directory in common edge cases.
- XerahS: Fixed the history editor bitmap lifetime issue so image data used by editor-related history flows is kept alive correctly instead of risking premature disposal.
- XerahS: Hardened plugin configuration provider path handling and improved async settings save notifications so plugin and settings flows fail less often in edge conditions.
- XerahS: Fixed SFTP key validation and RegionCapture XAML loading issues, closing off two separate startup/runtime correctness gaps in upload and capture flows.
- XerahS: Hardened Imgur explorer configuration handling, compact onboarding hotkey parsing, backup-settings failure exit code behavior, notification process timeout handling, and capture start delay overflow handling across the desktop app and supporting tooling.

## Build and Tooling

- XerahS: Added new regression coverage for workflow command validation, toast click routing, file helper mutations, worker task recording history, Linux startup handling, window detection, Windows clipboard image decoding, MCP object parameter validation, and several uploader/platform-specific fixes.
- XerahS: Reused cached OCR text in assistant flows and recorded a follow-up OCR cache review, showing some of the day's work was spent improving supporting automation behavior instead of only product-facing bug fixes.
- XerahS: The daily blog automation created the initial UTC+8 draft, and that draft was then merged from `upstream/develop` into the local `develop` branch before later fixes continued landing.

## Commits Reviewed

- `1dbed791` - Validate CLI workflow region and duration
- `d65a92ce` - [Docs] Ensure current and previous UTC+8 blog drafts.
- `ab819e59` - Merge remote-tracking branch 'upstream/develop' into develop
- `18e7a82a` - Fix toast timing validation
- `5bf764ee` - Fix file path mutation extension handling
- `b631b477` - Preserve uploader host in recording history
- `9a52702a` - Fix Linux startup desktop entry escaping
- `960d5c85` - Fix stale excluded window queries
- `c1586b98` - Restore clipboard DIB codec test coverage
- `25782ed6` - Fix Nextcloud relative path normalization
- `bf151879` - assistant: reuse cached OCR text
- `6d400276` - docs: record assistant OCR cache review
- `4f914a98` - Fix history editor bitmap lifetime
- `b57dea71` - Harden plugin configuration provider paths
- `ef5cd1e1` - Fix uploader fallback host attribution
- `6fbe4bc0` - Fix async settings save notifications
- `176a7490` - Fix SFTP key validation and RegionCapture XAML loading
- `6068f8eb` - Harden Imgur explorer config handling
- `1ff09ea2` - Fix compact onboarding hotkey parsing
- `b3e99868` - Fix MCP object params validation
- `a6baf846` - Fix backup-settings failure exit code
- `11d1207a` - Fix notification process timeout handling
- `d580f0fd` - Fix unique file path suffix handling
- `4b8069e2` - Fix capture start delay overflow handling

## Notes

- The verified work for this day is concentrated in XerahS itself; `ShareX.ImageEditor` and `XerahS.Editor` were not locally available to inspect for same-day history.
- Much of the day's progress went into regression-proofing existing behavior rather than shipping a single headline feature, which is reflected in the broad spread of focused bug-fix commits and accompanying tests.
- The assistant-related OCR cache work and docs follow-up were smaller than the platform and uploader fixes, but they show that the same day also included maintenance on the supporting automation/tooling path.
