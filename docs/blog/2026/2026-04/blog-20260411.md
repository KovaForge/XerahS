# XerahS Daily Blog Draft - 2026-04-11

Date: 2026-04-11
Time Zone: UTC+8
Status: Draft

## Summary

April 11 had only one committed XerahS repository change in UTC+8 history: the daily blog draft scaffolding commit. The working tree also contains an in-progress onboarding wizard refinement pass, so this draft records that local activity separately from shipped or committed work: the wizard UI is being simplified around dedicated controls, step validation is being tightened, and first-run display is being moved onto an async path after the main window opens.

## Features

- XerahS local working tree: The onboarding wizard layout is being refactored to use the dedicated `ProgressIndicator` and `HotkeyRecorder` controls instead of a large inline step-indicator template and static hotkey display.
- XerahS local working tree: The welcome step is being changed to a curated supported-language list with normalized codes, two-way selected-language option binding, and a fallback hint for unsupported system UI languages.
- XerahS local working tree: The OCR and upload steps are being moved toward direct option selection state, with OCR language chips tracking `IsSelected`, download-size totals derived from selected options, and uploader radio cards syncing back to the selected uploader id.

## Fixes

- XerahS local working tree: First-run onboarding is being moved away from a blocking `.Result` dialog call and toward an async `ShowOnboardingWizardAsync` helper invoked after the main window opens.
- XerahS local working tree: Step validation is being centralized through `SetValidationState`, with explicit validation feedback for hotkeys, save paths, uploader selection, and OCR language selection.
- XerahS local working tree: Save-location handling is being tightened to validate writable folders, update path previews when date-subfolder options change, and persist custom screenshot path settings when the step is not skipped.
- XerahS local working tree: Completion actions are being adjusted so "Open Settings" and "Take First Screenshot" complete the wizard before closing, with settings navigation deferred until after the dialog returns.

## Build and Tooling

- XerahS: The committed April 11 change only ensured that the current and previous UTC+8 blog draft files existed (`44377dcc`).
- XerahS local working tree: `WelcomeStepViewModel.Extensions.cs` has been removed as part of consolidating the welcome-step language logic back into the main step view model.

## Commits Reviewed

XerahS:

- `44377dcc` `[Docs] Ensure current and previous UTC+8 blog drafts.`

ShareX.ImageEditor:

- No April 11 UTC+8 commits were found in the available ShareX.ImageEditor checkouts.

XerahS.Editor:

- No April 11 UTC+8 commits were found in the available XerahS.Editor checkout.

## Notes

- The April 11 onboarding details are based on the current local working tree diff, not on a committed feature commit. Treat them as in-progress development notes until the onboarding changes are reviewed, built, committed, and pushed separately.
- The blog automation will commit only this markdown file; the existing onboarding source edits remain unstaged and are not part of the blog post commit.
- Because the only committed April 11 history was draft scaffolding, this post intentionally avoids claiming that the onboarding refinements have shipped.
