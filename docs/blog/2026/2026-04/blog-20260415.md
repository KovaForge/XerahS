# XerahS Daily Blog Draft - 2026-04-15

Date: 2026-04-15
Time Zone: UTC+8
Status: Draft

## Summary

2026-04-15 focused on tightening the desktop user flow around capture output and upload entry points. The team added a post-capture file-path copy action, made toast fallbacks show the saved path more clearly, fixed CLI text and piped uploads, and cleaned up Avalonia binding and placeholder issues while syncing the `ShareX.VideoEditor` submodule forward.

## Features

- XerahS: Added a "copy file path after capture" option in the desktop after-capture task settings so local file destinations can be copied directly into the clipboard as part of the capture workflow.
- XerahS: Updated toast fallback behaviour to show the file path in the header when richer toast metadata is unavailable, keeping the saved output location visible even in reduced notification scenarios.

## Fixes

- XerahS: Allowed CLI text uploads and standard input pipe uploads through the desktop command-line path so non-file upload automation works again.
- XerahS: Added compiled binding data types to the onboarding wizard AXAML so the Avalonia onboarding flow binds cleanly under the current compiled-binding expectations.
- XerahS: Replaced obsolete `TextBox.Watermark` usage with `PlaceholderText` in desktop views to stay compatible with Avalonia 11.x deprecation guidance.

## Build and Tooling

- XerahS: Updated the `ShareX.VideoEditor` submodule to the latest referenced commit on `develop`.
- XerahS: Carried forward the daily blog automation scaffolding commit that ensures both the current and previous UTC+8 draft files exist for scheduled documentation runs.
- XerahS: Merged upstream `develop` changes before the day's desktop fixes, keeping the branch aligned before the follow-up work landed.

## Commits Reviewed

- `b10ce59` `chore: merge upstream ShareX/XerahS changes`
- `ace0a90` `[Fix] replace obsolete TextBox.Watermark with PlaceholderText (Avalonia 11.x deprecation)`
- `6aea2a9` `[Docs] Ensure current and previous UTC+8 blog drafts.`
- `c1eb307` `[v0.22.2] [Fix] Add onboarding compiled binding data types`
- `03c1dd9` `Merge pull request #233 from KovaForge/develop`
- `313440e` `[v0.22.2] [Fix] Allow CLI text and pipe uploads`
- `c6cd601` `[v0.22.2] [UI] Add copy file path after capture option`
- `dc39c9a` `[v0.22.2] [UI] Show file path in toast header fallback`
- `55e479a` `[v0.22.2] [Chore] Update ShareX.VideoEditor submodule`

## Notes

- The local workspace did not include a checked-out `XerahS.Editor` repository, so this draft is grounded in the available `XerahS` history for the day.
- The `ShareX.VideoEditor` submodule pointer changed in XerahS, but this run did not inspect the target repository's standalone commit history because only the submodule update is present in the current workspace.
