# XerahS Daily Blog Draft - 2026-05-31

Date: 2026-05-31
Time Zone: UTC+8
Status: Draft

## Summary

2026-05-31 UTC+8 was a tightly focused XerahS maintenance day centered on FFmpeg recording reliability. The main code change wired the existing per-workflow FFmpeg executable override into the actual recording backends, so workflow video settings now control the FFmpeg binary used during recording instead of relying only on global discovery, and the follow-up docs commit corrected the Linux FFmpeg guide and clarified how custom override paths are expected to work. No commits were verified in ShareX.ImageEditor or XerahS.Editor during the same window.

## Features

- None. No net-new feature work was verified in this UTC+8 window.

## Fixes

- XerahS: wired the workflow-level FFmpeg override path into the recording pipeline so FFmpeg-based backends honor the binary selected in workflow video settings.
- XerahS: closed the mismatch between the workflow FFmpeg UI and backend behavior, including the Linux portal-backed FFmpeg recording path.
- XerahS: improved override-path handling by resolving the configured workflow path before backend fallback detection.

## Build and Tooling

- XerahS docs: corrected the FFmpeg guidance for Linux by spelling out backend priority on Wayland and X11, emphasizing real PipeWire capability checks, and clarifying custom FFmpeg override lookup order.
- No CI, release, packaging, or build-system commits were verified in this UTC+8 window.

## Commits Reviewed

- `96d254f8` - `[v0.23.0] [Fix] Wire workflow FFmpeg override into recording backends`
- `093f3d3a` - `[v0.23.0] [Docs] Correct FFmpeg Linux and override guidance`

## Notes

- This was a small, coherent FFmpeg-focused day: one runtime fix followed by one documentation correction.
- No ShareX.ImageEditor commits were verified in this UTC+8 day.
- No XerahS.Editor commits were verified in this UTC+8 day.
