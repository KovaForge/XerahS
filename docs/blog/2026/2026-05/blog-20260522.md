# XerahS Daily Blog Draft - 2026-05-22

Date: 2026-05-22
Time Zone: UTC+8
Status: Draft

## Summary

May 22 focused on two practical XerahS fixes: making CLI plugin bundling work more reliably for agent-hosted environments and unblocking the macOS update flow. The day also brought those changes together on `develop` with an upstream merge, while `ShareX.ImageEditor` and `XerahS.Editor` remained inactive for this UTC+8 day.

## Features

- XerahS: Added a manual `Update Now` action in the settings UI so update checks can be triggered directly instead of waiting for the automatic prompt flow.
- XerahS: Refreshed update-check configuration from current settings before running checks so manual and automatic update actions use the latest user configuration.

## Fixes

- XerahS: Fixed macOS update prompts so they can still open when there is no visible owner window, reducing cases where update notifications silently failed to appear.
- XerahS: Added detached update dialog coverage for accept and decline flows to lock in the repaired macOS prompt behavior.
- XerahS: Fixed CLI plugin packaging for agent-hosted scenarios so discovered plugins are bundled into output and publish layouts more consistently.

## Build and Tooling

- XerahS: Reworked CLI plugin bundling from a single hard-coded plugin path to discovered plugin projects, including recursive copying for plugin assets and runtime files.
- XerahS: Hardened macOS path resolution to prefer the native login home directory when host environments override `HOME`, which helps plugin and update-related file discovery behave predictably.
- XerahS: Merged the verified update-flow work into `develop` with `abda6a7d`.

## Commits Reviewed

- `3aeca5a4` `[v0.23.64] [Fix] Bundle CLI plugins for agent hosts`
- `84c05a67` `[v0.23.0] [Fix] Unblock macOS update prompts and add manual update action`
- `abda6a7d` `Merge remote-tracking branch 'upstream/develop' into develop`

## Notes

- ShareX.ImageEditor: No commits with a `2026-05-22` UTC+8 author date.
- XerahS.Editor: No commits with a `2026-05-22` UTC+8 author date.
- This post is based on git author dates only.

