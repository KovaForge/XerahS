# XerahS Daily Blog Draft - 2026-05-08

Date: 2026-05-08
Time Zone: UTC+8
Status: Draft

## Summary

XerahS continued the DXGI capture stabilization work on 2026-05-08 by cleaning up monitor-context lifetime management and by composing cursor imagery directly into DXGI region captures. The first change disposed replaced monitor contexts during strategy updates, and the second added a dedicated cursor composition path plus broader Windows capture tests so region shots can include the cursor and the backend can now truthfully advertise cursor capture support. ShareX.ImageEditor and XerahS.Editor again had no qualifying commits in the same UTC+8 window.

## Features

- XerahS: Added `DisposableContextDictionary` so replaced DXGI monitor contexts can be tracked and disposed consistently when the capture strategy refreshes its monitor state.
- XerahS: Added `DxgiCursorCompositionHelper` so DXGI region captures can explicitly composite the cursor into the captured bitmap rather than depending on unsupported native cursor capture behavior.
- XerahS: DXGI capability reporting now advertises cursor capture support because cursor composition is implemented in the region-capture path.

## Fixes

- XerahS: Disposed replaced DXGI monitor contexts during capture-strategy updates, preventing stale contexts from lingering after monitor refreshes.
- XerahS: Composed cursor pixels into DXGI region captures, fixing missing-cursor output for region shots while aligning capability reporting with the backend's real behavior.
- XerahS: Tightened the DXGI capture strategy around the new context-disposal and cursor-composition helpers so the region-capture path stays internally consistent after the 2026-05-07 fixes.

## Build and Tooling

- XerahS: Expanded `tests/XerahS.Tests/Platform/Windows/WindowsModernCaptureServiceTests.cs` to cover monitor-context replacement and cursor composition in DXGI region capture flows.
- XerahS: Added placement-oriented test coverage for DXGI cursor composition, including visible-cursor, off-screen, and default-size cursor cases.
- XerahS: Updated `tests/XerahS.Tests/XerahS.Tests.csproj` so the non-Windows test build includes `DisposableContextDictionary` and `DxgiCursorCompositionHelper` with the rest of the linked Windows capture sources.

## Commits Reviewed

- `b810b9bb` fix: dispose replaced dxgi monitor contexts
- `a346d781` fix: compose cursor in DXGI region capture

## Notes

- ShareX.ImageEditor had no qualifying commits in the 2026-05-08 UTC+8 author-date window, and the GitHub commit history for XerahS.Editor also returned no commits for the same interval.
- One of the two verified commits is a merge commit, so unrelated docs churn exists in the raw diff, but this post is intentionally limited to the coding activity for the date.
- This post reflects the committed activity visible so far on 2026-05-08 UTC+8; if later commits land on the same day, the file should be refreshed rather than duplicated.

