# XerahS Daily Blog Draft - 2026-05-29

Date: 2026-05-29
Time Zone: UTC+8
Status: Draft

## Summary

Development on 2026-05-29 UTC+8 centered on a pair of XerahS maintenance fixes in the early-morning window. The larger change makes history-editor annotation edits persist correctly when the user continues out of the editor, while the follow-up commit updates the ShareX.ImageEditor gitlink so XerahS can pick up a newer upstream effect-browser revision.

## Features

- None. The audited commits for this day are all fixes rather than new feature work.

## Fixes

- XerahS: `HistoryViewModel` now persists annotation edits after a history-editor continue flow, saving updated `.xann` sidecars, deleting the sidecar when the editor returns no annotations, and disposing the returned editor images only after persistence completes.
- XerahS tests: `HistoryEditorLaunchTests` now covers the modified-annotation continue path so sidecar persistence is verified by an automated test instead of relying on manual editor checks.
- XerahS: the `ShareX.ImageEditor` gitlink moved from `4998ef7` to `72ff9894` so the app tracks a newer upstream revision associated with effect-browser spacing work.

## Build and Tooling

- None. No build, tooling, packaging, or documentation-maintenance commits were recorded in the audited UTC+8 window.

## Commits Reviewed

- `85e3eac0` - `[v0.23.0] [Fix] Persist annotation edits after continue`
- `6f7700b7` - `[v0.23.0] [Fix] Update ImageEditor effect browser spacing`

## Notes

- Verified against the local XerahS repository plus temporary local clones of ShareX.ImageEditor and XerahS.Editor under `/tmp` so the audit is grounded in real git history for all three source repositories.
- XerahS.Editor had no commits in the 2026-05-29 UTC+8 author-date window.
- ShareX.ImageEditor had no commits in the 2026-05-29 UTC+8 author-date window; the XerahS repo only updated its gitlink to `72ff9894` during that period.
- The `72ff9894` gitlink target is not reachable from a fresh clone of the current ShareX.ImageEditor `develop` branch, so this post only claims the verified XerahS-side submodule update and does not restate unverified upstream code details.
