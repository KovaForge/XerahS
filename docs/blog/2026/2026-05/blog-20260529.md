# XerahS Daily Blog Draft - 2026-05-29

Date: 2026-05-29
Time Zone: UTC+8
Status: Draft

## Summary

Development on 2026-05-29 UTC+8 centered on a pair of XerahS maintenance fixes in the early-morning window plus one matching ShareX.ImageEditor update. The larger XerahS change makes history-editor annotation edits persist correctly when the user continues out of the editor, and the follow-up gitlink update pulls in the verified upstream effect-browser spacing fix.

## Features

- None. The audited commits for this day are all fixes rather than new feature work.

## Fixes

- XerahS: `HistoryViewModel` now persists annotation edits after a history-editor continue flow, saving updated `.xann` sidecars, deleting the sidecar when the editor returns no annotations, and disposing the returned editor images only after persistence completes.
- XerahS tests: `HistoryEditorLaunchTests` now covers the modified-annotation continue path so sidecar persistence is verified by an automated test instead of relying on manual editor checks.
- ShareX.ImageEditor: `EffectBrowserPanel.axaml` restores effect-browser tile spacing for standalone hosts.
- XerahS: the `ShareX.ImageEditor` gitlink moved from `4998ef7` to `72ff9894` so the app picks up that upstream spacing fix.

## Build and Tooling

- None. No build, tooling, packaging, or documentation-maintenance commits were recorded in the audited UTC+8 window.

## Commits Reviewed

- `85e3eac0` - `[v0.23.0] [Fix] Persist annotation edits after continue`
- `72ff9894` - `[Fix] Restore effect browser tile spacing in standalone hosts`
- `6f7700b7` - `[v0.23.0] [Fix] Update ImageEditor effect browser spacing`

## Notes

- Verified against the local XerahS repository, the initialized workspace `ShareX.ImageEditor` submodule, and a temporary local clone of XerahS.Editor under `/tmp`.
- XerahS.Editor had no commits in the 2026-05-29 UTC+8 author-date window.
- The initialized `ShareX.ImageEditor` submodule resolves `72ff9894` from its configured remote, so the effect-browser spacing fix and the XerahS gitlink update are both directly auditable.
