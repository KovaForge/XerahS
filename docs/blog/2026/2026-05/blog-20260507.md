# XerahS Daily Blog Draft - 2026-05-07

Date: 2026-05-07
Time Zone: UTC+8
Status: Draft

## Summary

XerahS spent May 7 hardening plugin dependency resolution and tightening DXGI region-capture correctness on Windows. The plugin loader started enforcing dependency public key identity, matching shared dependencies case-insensitively, and ignoring invalid fallback assemblies before the capture pipeline fixes moved on to scaled crop rectangles, cursor-advertising behavior, and rotated region mapping. The day also included blog-draft maintenance in the XerahS docs tree, while ShareX.ImageEditor and the available local `_tmp_XerahS.Editor` checkout had no qualifying UTC+8 author-dated commits.

## Features

- XerahS: Added broader regression coverage around plugin dependency resolution and DXGI capture behavior so contributor-facing tests now exercise identity matching, fallback filtering, crop scaling, cursor capability reporting, and rotation handling.
- XerahS: Introduced dedicated DXGI helper types for capabilities and rotation handling to keep the Windows capture path easier to verify and evolve.

## Fixes

- XerahS: Required plugin dependencies to match the expected public key identity instead of accepting assemblies that only matched by simple name.
- XerahS: Normalized shared plugin dependency lookups so case differences no longer break resolution on Windows and other case-preserving file systems.
- XerahS: Ignored invalid fallback assemblies during plugin probing rather than letting bad candidates poison the resolution path.
- XerahS: Corrected DXGI crop rectangle scaling so region captures stay aligned after DPI and monitor-scaling adjustments.
- XerahS: Stopped advertising DXGI cursor capture support in paths that could not yet compose the cursor correctly.
- XerahS: Mapped rotated DXGI region captures back into the expected coordinates so portrait and rotated-monitor captures no longer land in the wrong area.

## Build and Tooling

- XerahS: Expanded automated tests for plugin loading and modern Windows capture scenarios to keep the warnings-as-errors build protected by real regression coverage.
- XerahS: Refreshed the May 6 blog draft twice as the documentation trail was refined during the day.
- XerahS: Created the initial May 7 blog draft late in the day, but that placeholder incorrectly marked the date as a no-activity day and has now been replaced with commit-grounded content.

## Commits Reviewed

- `c2d788e` - `fix: require plugin dependency public key identity`
- `a61b3c7` - `fix: match shared plugin dependencies case-insensitively`
- `f051dd5` - `fix: ignore invalid plugin fallback assemblies`
- `8194d78` - `[v0.22.217] [Docs] Refresh 2026-05-06 plugin diagnostics blog draft.`
- `0b71039` - `fix: scale dxgi crop rectangles`
- `18af748` - `fix: stop advertising dxgi cursor capture`
- `484b7d9` - `fix: map dxgi rotated region captures`
- `62a11bb` - `[v0.22.189] [Docs] Refresh 2026-05-06 docs-maintenance blog draft.`
- `eb555a8` - `[v0.22.189] [Docs] Add 2026-05-07 no-activity blog draft.`

## Notes

- ShareX.ImageEditor and the available local `_tmp_XerahS.Editor` checkout had no qualifying author-dated commits in the 2026-05-07 UTC+8 window.
- The DXGI work was still in motion at the end of the day; disposal fixes and cursor composition landed in the next UTC+8 day.
- The earlier no-activity placeholder for May 7 was not supported by the XerahS git history and should be treated as superseded by this revised draft.

