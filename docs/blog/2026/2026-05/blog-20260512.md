# XerahS Daily Blog Draft - 2026-05-12

Date: 2026-05-12
Time Zone: UTC+8
Status: Draft

## Summary

May 12 combined a substantial Send-to workflow upgrade with a platform-specific startup fix for macOS. The main product work added remembered Send-to choices, folder-expansion and batch-execution policies, and matching tests, while the follow-up fix made silent tray startup hide the Dock icon correctly on macOS. The day then closed with XIP documentation cleanup and the publication of the latest daily blog drafts, so both the planning layer and the development log caught up with the shipped changes.

## Features

- XerahS: Implemented Send-to post-v1 policies end to end, adding remembered choices by selection scope, configurable folder expansion rules, and batch execution policies for multi-file image actions.
- XerahS: Extended the Send-to prompt and settings UI so desktop users can review remembered-choice behavior, clear saved Send-to decisions, and control how folder-backed and large-batch sends are handled.
- XerahS: Added the shared `SendToPolicyResolver`, expanded the persisted application config and platform abstractions, and landed dedicated tests to keep the new Send-to policy flow stable across desktop, CLI, and headless paths.

## Fixes

- XerahS: Fixed macOS tray startup so launching in silent/tray mode can switch the app into menu-bar-only presentation and keep it out of the Dock while still restoring normal behavior when the setting is disabled.
- XerahS: Hardened the tray and silent-run settings interaction so enabling one mode keeps the other prerequisites in sync instead of leaving the UI in an invalid combination.

## Build and Tooling

- XerahS: Normalized XIP proposal statuses across the proposal set so planning documents consistently reflect their current lifecycle state.
- XerahS: Completed the XIP0057 implementation notes to document the shipped Send-to defaults, remembered-choice handling, and folder-policy behavior.
- XerahS: Tightened the XIP execution prompt so batch work requires a push and explicit issue closure instead of leaving those workflow steps implicit.
- XerahS: Refreshed the XIP0039 ImageEditor refactor priorities document to reflect the current sequencing of that design work.
- XerahS: Published the refreshed May 11 release-wrap draft and the initial May 12 Send-to draft late in the UTC+8 day so the docs trail matched the verified commit history.

## Commits Reviewed

- `c1042739` [v0.22.257] [Docs] Add 2026-05-12 Send-to policies blog draft.
- `f770ed76` [v0.22.257] [Docs] Add 2026-05-11 release-wrap blog draft.
- `ef0af0bd` [v0.22.257] [Docs] Normalize XIP proposal statuses
- `7c4b11dd` [v0.22.257] [Feature] Implement Send-to post-v1 policies
- `5650c60e` [v0.22.257] [Docs] Complete XIP0057 implementation notes
- `be83d51f` [v0.22.257] [Docs] Require XIP batch push and issue closure
- `7bff961e` [v0.22.257] [Fix] Hide macOS Dock icon for tray startup (#252)
- `991e26a2` Update XIP0039-imageeditor-refactor-priorities.md

## Notes

- The verified commit set for this UTC+8 day came only from `XerahS`; no matching commits were found in `ShareX.ImageEditor` or the available `XerahS.Editor` checkout.
- The Send-to feature work was broader than a single UI tweak: it touched core policy resolution, persisted config, prompt behavior, settings surfaces, headless services, and automated tests in one coordinated pass.
- There were no qualifying build-pipeline or packaging commits on this day; the tooling section is documentation- and workflow-focused rather than CI-focused.
- The available `_tmp_XerahS.Editor` checkout currently has unrelated staged deletions, but none of that local state is represented by committed activity for May 12 and it was excluded from the post.
