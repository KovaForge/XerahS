# XerahS Daily Blog Draft - 2026-05-09

Date: 2026-05-09
Time Zone: UTC+8
Status: Draft

## Summary

May 9 broadened from DXGI cleanup into release preparation, plugin tooling, update behavior, and a few targeted desktop fixes. The day started with more defensive cleanup around output discovery and cursor replacement, then added a first-party OpenClaw upload-plugin exporter, synchronized region annotation tools across monitors, adjusted after-capture preview fitting, and tightened startup and update-channel behavior ahead of the next release. Release automation and changelog tooling also moved forward, including two release CI commits and follow-up work to attach ImageEditor artifacts during release prep.

## Features

- XerahS: Added a CLI-based OpenClaw upload-plugin exporter, including a dedicated exporter implementation and test coverage, so first-party plugin packages can be generated from the XerahS toolchain.
- XerahS: Added `OPENCLAW_PLUGIN.md` and a first-party integration prompt template to document how plugin packaging and related contributor workflows should be driven.
- XerahS: Introduced `RegionCaptureAnnotationToolCoordinator` so annotation-tool selection can stay synchronized across region-capture overlays on different monitors.
- XerahS: Added configurable pre-release update-source support, including new settings and update-service handling for selecting where prerelease checks should resolve.

## Fixes

- XerahS: Added fallback handling when DXGI output enumeration initially comes back empty, improving full-screen capture resilience when Desktop Duplication misses outputs.
- XerahS: Cleaned up cursor-replacement failures so copied system cursors are destroyed promptly and restore logic only runs when replacement actually succeeded.
- XerahS: Centralized DXGI output-enumeration cleanup, reducing the chance of leaked resources during setup and teardown.
- XerahS: Stabilized the debug build with a small follow-up in `WindowsModernCaptureService`.
- XerahS: Kept region annotation tools in sync across monitors, preventing multi-window overlay state from drifting during region capture.
- XerahS: Updated the after-capture preview to fit to the control more reliably.
- XerahS: Resolved startup log issues by hardening enum/settings loading and update-service logging paths.
- XerahS: Disabled the clipboard viewer monitor by default.
- XerahS: Added prerelease update-source configuration so prerelease builds can point at the intended update feed instead of assuming a single source.

## Build and Tooling

- XerahS: Added targeted regression coverage for DXGI output fallback, cursor replacement cleanup, enumeration cleanup, OpenClaw plugin export, region annotation tool coordination, startup enum loading, clipboard-monitor defaults, and prerelease update-source handling.
- XerahS: Linked new helper classes such as `CursorReplacementHelper` and `DxgiOutputEnumerationCleanupHelper` into the test project so the non-Windows test build continues to exercise the Windows capture code paths.
- XerahS: Recorded the hourly push blocker in `docs/reports/hourly_review_tracker.md`, then continued release-prep work instead of dropping the audit trail.
- XerahS: Cut release CI commits for `v0.22.235` and `v0.22.236`, then adjusted the release sequence so ImageEditor artifacts are attached during release prep.
- XerahS: Reworked changelog tooling and docs so generated changelog entries link tags only when a real tag exists, which makes release-prep output cleaner.

## Commits Reviewed

- `9f9f850f` docs: merge upstream blog drafts
- `4717aff8` fix: fallback when dxgi misses outputs
- `925b42e7` fix: clean up dxgi cursor replacement failures
- `a7855d18` docs: record hourly push blocker
- `6ab2a45b` [v0.22.233] [Fix] Stabilize debug build
- `fb7dbdac` fix: dispose dxgi enumeration resources
- `6ea1bcfd` [v0.22.228] [Feature] Export OpenClaw upload plugin
- `f29542a0` [v0.22.228] [Docs] Add OpenClaw plugin setup guide
- `a27233a3` [v0.22.228] [Fix] Sync region annotation tools across monitors
- `8084cd29` [v0.22.228] [Docs] Add first-party integration prompt template
- `9bf074ca` [v0.22.228] [Fix] Fit after capture preview to control
- `52403100` [v0.22.235] [CI] Release v0.22.235
- `54cbce83` [v0.22.236] [CI] Release v0.22.236
- `d84cc957` [v0.22.237] [CI] Attach ImageEditor during release prep
- `83b367ab` [v0.22.237] [Fix] Resolve startup log issues
- `437b49b6` [v0.22.237] [Docs] Link changelog tags and omit hashes
- `0ea08f80` [v0.22.237] [Docs] Update changelog for release prep
- `314700ee` [v0.22.237] [Docs] Link changelog only for existing tags
- `c7fe1211` [v0.22.237] [Fix] Disable clipboard viewer monitor by default
- `b96648a4` [v0.22.237] [Fix] Configure prerelease update source

## Notes

- The local `ShareX.ImageEditor` checkout at `C:\Users\liveu\source\repos\ShareX Team\ShareX.ImageEditor` had no commits whose author date fell on 2026-05-09 UTC+8.
- The local `XerahS.Editor` checkout at `C:\Users\liveu\source\repos\ShareX Team\_tmp_XerahS.Editor` also had no qualifying commits in the 2026-05-09 UTC+8 window.
- There is an uncommitted local modification in `src/desktop/app/XerahS.UI/Views/ApplicationSettingsView.axaml` from 2026-05-09, but it is intentionally excluded from this post because this automation is grounded on committed history only.
