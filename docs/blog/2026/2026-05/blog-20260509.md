# XerahS Daily Blog Draft - 2026-05-09

Date: 2026-05-09
Time Zone: UTC+8
Status: Draft

## Summary

May 9 stayed focused on hardening the Windows DXGI capture path after the previous day's cursor and frame-acquisition work. XerahS added fallback handling when Desktop Duplication reports no outputs, cleaned up cursor replacement failures so temporary system cursors do not leak, disposed enumeration resources more aggressively, and made a small follow-up fix to keep the debug build healthy. The only same-day non-code activity in scope was documentation around the queued push blocker and the merge of blog drafts from upstream; ShareX.ImageEditor and the checked local `_analysis\XerahS.Editor` repository had no verified UTC+8 author-dated commits for this day.

## Features

- XerahS: Added a fallback path for DXGI full-screen capture when output enumeration initially comes back empty, giving the capture service another recovery route before it gives up on a monitor.
- XerahS: Introduced `CursorReplacementHelper` and `DxgiOutputEnumerationCleanupHelper` to centralize low-level cleanup responsibilities that had been spread through `WindowsModernCaptureService`.

## Fixes

- XerahS: Full-screen DXGI capture now falls back when duplication misses outputs, improving resilience when monitor/output discovery is temporarily incomplete.
- XerahS: Failed cursor replacement attempts now destroy the copied system cursors immediately, and the restore/sleep path only runs when at least one cursor was actually replaced.
- XerahS: DXGI output enumeration now disposes temporary COM resources more consistently during setup and teardown, reducing the chance of leaks around enumeration and monitor refresh failures.
- XerahS: The debug-build stabilization follow-up replaced a discard-style `using` statement with an explicitly named resource variable before `QueryInterface`, addressing a build/debug hygiene issue in `WindowsModernCaptureService`.

## Build and Tooling

- XerahS: Added targeted regression coverage in `tests/XerahS.Tests/Platform/Windows/WindowsModernCaptureServiceTests.cs` for empty-output fallback, cursor replacement cleanup, and DXGI enumeration resource disposal.
- XerahS: Updated `tests/XerahS.Tests/XerahS.Tests.csproj` to include the new cleanup helpers in the linked Windows capture sources used by the cross-platform test project.
- XerahS: Merged upstream blog-draft changes into the local docs tree so the May 7 and May 8 posts stayed aligned before this day's draft was added.
- XerahS: Recorded the day's push/authentication blocker in `docs/reports/hourly_review_tracker.md`, preserving the release-process context alongside the capture fixes.

## Commits Reviewed

- `9f9f850f` docs: merge upstream blog drafts
- `4717aff8` fix: fallback when dxgi misses outputs
- `925b42e7` fix: clean up dxgi cursor replacement failures
- `a7855d18` docs: record hourly push blocker
- `6ab2a45b` [v0.22.233] [Fix] Stabilize debug build
- `fb7dbdac` fix: dispose dxgi enumeration resources

## Notes

- ShareX.ImageEditor and the checked local `C:\Users\liveu\source\repos\KovaForge\_analysis\XerahS.Editor` checkout had no commits in the 2026-05-09 UTC+8 author-date window.
- The debug-build stabilization commit was intentionally small and targeted; most of the day's substantive engineering work remained in DXGI cleanup and failure-handling internals rather than broad UI changes.
- This post covers the checked history visible so far for 2026-05-09 UTC+8. If additional same-day commits land later, the file should be refreshed in place instead of duplicated.

