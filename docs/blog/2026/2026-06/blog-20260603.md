# XerahS Daily Blog Draft - 2026-06-03

Date: 2026-06-03
Time Zone: UTC+8
Status: Draft

## Summary
June 3 picked up real XerahS maintenance after an early no-activity draft had already been generated. The substantive code change tightened uploader-instance cleanup logging so removing a default uploader now emits the same stale-default diagnostic trail as the other default-management paths, and the branch also absorbed upstream OCR onboarding language-selection work with expanded regression coverage.

The rest of the day focused on project bookkeeping: tracker/state documentation was refreshed for the OCR onboarding parity change and the stale-default cleanup fix, and the daily blog automation backfilled June 2 and refreshed June 3 in the docs tree. ShareX.ImageEditor and XerahS.Editor had no author-dated commits in the audited UTC+8 window.

## Features
- XerahS: The branch absorbed upstream OCR language-selection parity work so the OCR tool can honor a persisted onboarding language, trim whitespace around stored tags, and fall back predictably when the saved language is unavailable.

## Fixes
- XerahS: `InstanceManager.RemoveInstance` now logs when it clears a stale default uploader mapping because the selected instance was deleted, bringing that path in line with the existing `GetDefaultInstance` and category-change cleanup diagnostics.
- XerahS: Regression tests now cover stale-default removal logging and the OCR language-selection cases around preferred persisted languages, English fallback, first-language fallback, and whitespace-normalized language tags.

## Build and Tooling
- XerahS: `Directory.Build.props` advanced from `0.23.87` to `0.23.88` alongside the stale-default uploader cleanup fix.
- XerahS: `docs/reports/hourly_review_tracker.md` and `docs/reports/hourly_review_state.json` were updated twice to record the OCR onboarding parity follow-up and the stale-default cleanup logging fix.
- XerahS: Daily blog docs maintenance continued with the June 2 backfill and an initial June 3 draft that was later superseded by the real same-day activity captured here.

## Commits Reviewed
- `f30c384` Merge remote-tracking branch `upstream/develop` into `develop`
- `a82ec14` [v0.23.87] [Docs] Record OcrViewModel onboarding language-parity fix in tracker and state
- `6705b0e` [v0.23.88] [Fix] Log stale default uploader cleanup on RemoveInstance for parity with GetDefaultInstance and category-change UpdateInstance paths
- `f509f7e` [v0.23.88] [Docs] Record RemoveInstance stale-default cleanup logging fix in tracker and state
- `2d11215` [v0.23.85] [Docs] Add 2026-06-02 docs-maintenance blog draft.
- `6376396` [v0.23.85] [Docs] Add 2026-06-03 no-activity blog draft.

## Notes
- ShareX.ImageEditor had no 2026-06-03 UTC+8 commits and no local work-in-progress diff to incorporate.
- XerahS.Editor had no 2026-06-03 UTC+8 commits and no local work-in-progress diff to incorporate.
- This file was originally created as a no-activity placeholder earlier on June 3; the placeholder has been replaced so the published day matches the verified commit history.
