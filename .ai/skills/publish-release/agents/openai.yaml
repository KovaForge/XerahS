interface:
  display_name: "XerahS Release Bump Tag"
  short_description: "Run maintenance first, then bump version, sync Chocolatey nuspec metadata, commit/push/tag, monitor release every 2 minutes, ensure standard release notes, and optionally stamp Flathub plus PPA/COPR/OBS candidates without publishing"
