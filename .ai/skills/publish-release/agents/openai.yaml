interface:
  display_name: "XerahS Release Bump Tag"
  short_description: "Run maintenance first, then bump version, sync Chocolatey nuspec metadata, commit/push/tag, monitor release every 2 minutes, ensure standard release notes, optionally stamp Flathub, and optionally stamp or publish PPA/COPR/OBS"
